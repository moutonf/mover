package com.mover.vehicle;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

/**
 * Activity for testing. Not in use in current version of application
 */
public class accidentActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(com.mover.vehicle.R.layout.activity_accident);
    }
}
